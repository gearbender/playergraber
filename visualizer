local mon = peripheral.find("monitor")
mon.setTextScale(0.5)
local mw, mh = mon.getSize()
local palette = "0123456789abcdef"
for i = 1, mw do
    local idx = (i + offset) % 16
    bg = bg .. "0123456789abcdef":sub(idx + 1, idx + 1)
end
mon.clear()
mon.setCursorPos(1,mh)
mon.blit(
  string.rep(" ",mw),
  string.rep("0",mw),
  bg
)
