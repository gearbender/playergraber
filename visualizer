local mon = peripheral.find("monitor")
mon.setTextScale(0.5)
local mw, mh = mon.getSize()
mon.clear()
mon.setCursorPos(1,mh)
mon.blit(
  string.rep(" ",mw),
  string.rep("0",mw),
  string.rep("1",mw)
)
